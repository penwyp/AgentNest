# AgentNest AGENTS.md

> 本项目一般性执行原则。后续新原则按章节追加，保持精炼。

## 1. 工程与质量

- 分层架构（Presentation → Application → Domain → Infrastructure），UI 只消费不可变快照。
- 设计语言唯一真源 `DESIGN.md`：原生性能为第一约束（禁多层材质、多层阴影、循环动画）。
- 提交前 `make check` 全绿（build/test/e2e/check）。
- 变更先确认：性能与架构改动先给方案，确认后实施；实验改动在独立分支验证，验收后再合入。未经确认不修改运行中程序。
- 不创建未经要求的备份分支；不伪造结果与进度；清理临时产物。
- 本地化：所有用户可见文案经 `model.localized`；新 key 提供 en/zh-Hans 译文。

## 2. 交互响应优先

任何交互按三阶段响应：

1. **交互反馈**：当前/下一帧出现（选中、按压、展开等，≤16ms）；
2. **页面框架**：骨架屏/标题先出现，零计算（≤100ms）；
3. **内容填充**：派生计算后台完成，异步发布。

原则：

- 交互回调中禁止同步重活（排序/过滤/格式化/文件 I/O/数据库读取）。
- 视觉状态（选中/展开/hover）用本地 `@State` 即时反馈；模型属性只做导航与数据源。不要把选中态与整页重建放进同一事务。
- 后台刷新保留旧结果可交互，不整页清空重建。
- 反馈反映真实状态，不伪造进度。

## 3. 可落实手段

- **骨架屏**：框架视图与内容视图拆分；骨架精确镜像最终布局（分组/行数/尺寸/间距），固定尺寸防跳动；不伪造数据；装饰对无障碍隐藏。
- **派生计算**：body 内一次计算并复用；按 `(snapshot.generation + 参数)` 签名缓存；大数据计算移入后台 Task。
- **后台任务**：`Task.detached(.utility)` + 主线程发布 + generation 校验丢弃迟到结果；高频输入节流/单飞/取消旧任务。
- **观察粒度**：视图只读所需属性；高频属性（如活动采样）不得引发无关页面重算。
- **列表**：稳定 `Identifiable`；长列表限行数；行内控件轻量，禁自造 `DragGesture(minimumDistance: 0)`。
- **动画图表**：动画只在交互时；采样类图表不带动画；图表单 Path；切换过渡 0.2s 并遵循 Reduce Motion。

## 4. 性能验收（UI 改动合入前）

- 点击反馈首帧；页面框架 ≤100ms；切页主线程无同步 O(n)。
- 空闲 CPU <1%；后台重任务（如 SQLite 全表读）必须节流或缓存。
- 验证方式：`sample` 主线程采样 + `make test` + `make e2e`。
